import React from 'react';
import LoginAuth from '../components/auth/Login';

const Login = () => {
    return (
        <div>
            <LoginAuth />
        </div>
    );
};

export default Login;